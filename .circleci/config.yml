version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout

      - run:
          name: "Build image"
          command: echo ./build.sh

      - store_artifacts:
          path: pi-gen/deploy

      - run:
          name: "Test image"
          command: ./test.sh
